# encoding: utf-8

data_dir = "rrd_data"
# Path to document root
html_dir = "/var/www/latency"
# Image dir, should be inside html_dir
graph_dir = "/var/www/latency/img"
# Relative path on browser
relative_path = "/latecy/"

probes = {
    'ping': {
        'type': 'ping',
        'protocol': 4
    },
    'ping6': {
        'type': 'ping',
        'protocol': 6
    },
    'dns': {
        'type': 'dns',
        'query': 'google.com',
        'protocol': 'udp',
        'method': 'A'
    },
    'dns6': {
        'type': 'dns',
        'query': 'google.com',
        'protocol': 'udp',
        'method': 'AAAA'
    }
}

# configure targets
hosts = {
    'google.com': {'probes': ['ping', 'ping6']},
    '8.8.8.8': {'probes': ['dns']}
}

